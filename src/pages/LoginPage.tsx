import { FormEvent,useState } from "react"; import {login} from "../services/api"; import type {AuthSession,Role} from "../types";
export default function LoginPage({onLogin}:{onLogin:(session:AuthSession)=>void}){const[role,setRole]=useState<Role>("developer"),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false);
async function submit(e:FormEvent){e.preventDefault();setError("");const email=username.trim().toLowerCase();if(!email.endsWith("@mashreq.com"))return setError("User ID must end with @mashreq.com");setLoading(true);try{onLogin(await login(email,password,role))}catch(x){setError(x instanceof Error?x.message:"Login failed")}finally{setLoading(false)}}
return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="brand"><div className="brand-mark">N</div><div className="brand-copy"><strong>Neocorp DevOps</strong><span>SELF-SERVICE PORTAL</span></div></div><h1>Welcome back</h1><p>Sign in with your authorized Mashreq portal account.</p><label>Role<select value={role} onChange={e=>setRole(e.target.value as Role)}><option value="developer">Developer</option><option value="devops">DevOps</option></select></label><label>User ID<input type="email" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" placeholder="name@mashreq.com" required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password" placeholder="Enter your password" required/></label>{error&&<div className="form-error">{error}</div>}<button className="primary-button" disabled={loading||!username.trim()||!password}>{loading?"Signing in...":"Sign in"}</button></form></main>}
